'use client';

// This component has been disabled to remove yellow/amber tint from the site
// The component created metallic gold textures that overlaid the entire page
// Keeping the file for future reference but returning null to disable the effect

export default function ScrollTexture() {
  // Disabled - returning null to remove yellow/amber overlay
  return null;
}